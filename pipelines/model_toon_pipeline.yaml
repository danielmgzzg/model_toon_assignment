# PIPELINE DEFINITION
# Name: model-toon-pipeline
# Description: A Logistic Regression pipeline.
components:
  comp-extract:
    executorLabel: exec-extract
deploymentSpec:
  executors:
    exec-extract:
      container:
        command:
        - python
        - extraction.py
        image: model_toon_pipeline:tilt-52acb3906657560e
pipelineInfo:
  description: A Logistic Regression pipeline.
  name: model-toon-pipeline
root:
  dag:
    tasks:
      extract:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-extract
        taskInfo:
          name: extract
schemaVersion: 2.1.0
sdkVersion: kfp-2.4.0
